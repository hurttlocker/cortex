## Short Ops Keep Examples

OpenClaw gateway management commands:
- openclaw gateway status
- openclaw gateway restart

Cortex import command:
`cortex import <path> --recursive --extract`

Git push command:
`git push origin main`

Go test command:
`go test ./...`

Cortex search order:
memory_search before cortex, then Engram and QMD.

db_size_high alert means storage passed threshold and maintenance is needed.

API endpoint:
`GET /api/mobile/stream?sessionKey=agent:main:main`

Config snippet:
`DATABASE_URL=postgres://user:pass@host/db`

Runtime rule:
Never push live. Create PRs only.
