# Cortex v1.2.1 Release Notes

Date: 2026-03-01
Tag: `v1.2.1`

## Summary

v1.2.1 is the Adoption Sprint release. First-time user experience overhaul — every error is now actionable, setup is one command, and the README gets you running in 60 seconds.

## Changes

- **`cortex init`** -- First-time setup wizard: auto-detects LLM keys (OpenRouter, Google, OpenAI) + Ollama, writes config.yaml, creates DB. `-y` flag for CI. (#276, #283)
- **README quickstart** -- Now starts with `cortex init`, adds offline callout, better next-steps flow with semantic search setup (#277, #284)
- **Actionable error messages** -- 15 new hint categories in `remediationHint()`: Ollama unreachable, unknown embed/LLM providers, API key issues, rate limits, auth failures, timeouts, config errors, network issues. 5 inline messages improved with specific env vars + silence flags (#278, #281)
- **CONTRIBUTING.md + examples/** -- Refreshed contributor guide with prerequisites, code style, PR template. 4 example setups: local-first, openclaw-agent, obsidian-sync, multi-agent (#279, #285)
- **Version check in `cortex doctor`** -- Compares running binary against latest GitHub release, cached 24h in `~/.cortex/.version-cache`. Graceful offline fallback (#280, #282)

## Stats

- 5 PRs, +289 lines, all tests green, zero regressions
- All 890+ tests pass across 17 packages
