# v0.3.4-rc1 Release Artifact Smoke (darwin-amd64)

Date: 2026-02-20

## Commands run
```bash
gh release download v0.3.4-rc1 --repo hurttlocker/cortex --pattern "cortex-darwin-amd64.tar.gz" --pattern "checksums.txt"
expected="$(grep 'cortex-darwin-amd64.tar.gz' checksums.txt | awk '{print $1}')"
actual="$(shasum -a 256 cortex-darwin-amd64.tar.gz | awk '{print $1}')"

tar -xzf cortex-darwin-amd64.tar.gz
chmod +x cortex
./cortex version
./cortex codex-rollout-report --help ; echo $?
./cortex codex-rollout-report --file telemetry.jsonl --warn-only=false ; echo $?
```

## Results
- checksum match: ✅
  - expected: `6beffa8dcdae1d82dd701b0a1c1c5969bf02f488c7d703f34abfbe767b7d07cd`
  - actual:   `6beffa8dcdae1d82dd701b0a1c1c5969bf02f488c7d703f34abfbe767b7d07cd`
- `./cortex version`: `cortex 0.3.4-rc1` ✅
- `./cortex codex-rollout-report --help` exit: `0` ✅
- strict fixture (`--warn-only=false`) exit: `2` ✅
- strict fixture output includes guardrail `WARN:` lines ✅

## Re-validation (2026-02-20)
A second independent run reproduced the same results:

- checksum expected/actual: match ✅
- help exit code: `0` ✅
- strict fixture exit code: `2` ✅
- strict fixture output: includes both expected `WARN:` lines ✅
