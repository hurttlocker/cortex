# Cortex Environment Configuration
# Copy to .env and fill in your values.
# None of these are required — Cortex works fully offline with just file imports.
# Connectors and embedding providers use these when configured.

# ─── Database ─────────────────────────────────────────────
# Override default database path (~/.cortex/cortex.db)
# CORTEX_DB=~/.cortex/cortex.db

# ─── Embedding Providers ──────────────────────────────────
# Ollama (local, free — recommended default)
# Just run: ollama pull nomic-embed-text
# No env var needed — Cortex talks to localhost:11434 by default.

# OpenAI-compatible API (for cloud embeddings)
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1

# ─── Reasoning Providers ─────────────────────────────────
# For `cortex reason` with cloud LLMs. Any OpenAI-compatible API works.

# Google Gemini (via AI Studio)
# GOOGLE_API_KEY=AIza...

# DeepSeek
# DEEPSEEK_API_KEY=sk-...

# OpenRouter (access to 100+ models)
# OPENROUTER_API_KEY=sk-or-v1-...

# ─── Cortex Connect: GitHub ──────────────────────────────
# Required for: cortex connect add github
# Get a token: https://github.com/settings/tokens
# Scopes needed: repo (for private repos) or public_repo (for public only)
#
# Option 1: Pass in connector config (recommended)
#   cortex connect add github --config '{"token": "ghp_...", "repos": ["owner/repo"]}'
#
# Option 2: Use environment variable (picked up automatically if no token in config)
# GITHUB_TOKEN=ghp_...

# ─── Cortex Connect: Gmail (coming soon) ─────────────────
# Required for: cortex connect add gmail
# Requires Google OAuth 2.0 credentials (not just an API key).
#
# Setup steps:
#   1. Go to https://console.cloud.google.com/apis/credentials
#   2. Create an OAuth 2.0 Client ID (type: Desktop App)
#   3. Download the JSON credentials file
#   4. Set the path below OR pass it in connector config
#
# GMAIL_CREDENTIALS_FILE=~/.cortex/gmail-credentials.json
#
# On first sync, Cortex will open a browser for OAuth consent.
# The refresh token is stored in the connector config (encrypted at rest).

# ─── Cortex Connect: Google Calendar (coming soon) ───────
# Uses the same OAuth credentials as Gmail.
# GOOGLE_CREDENTIALS_FILE=~/.cortex/google-credentials.json

# ─── Cortex Connect: Google Drive (coming soon) ──────────
# Uses the same OAuth credentials as Gmail/Calendar.
# Scopes: drive.readonly (default) or drive (for full access)

# ─── Cortex Connect: Slack (coming soon) ─────────────────
# Required for: cortex connect add slack
# Create a Slack App: https://api.slack.com/apps
# Scopes needed: channels:history, groups:history, im:history, users:read
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...  (optional, for socket mode)
