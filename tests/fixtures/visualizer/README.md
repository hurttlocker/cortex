# Visualizer Fixtures

These fixtures are golden examples for v1 visualizer contracts.

- `canonical-v1.json` — canonical Cortex read-model snapshot
- `obsidian-adapter-v1.json` — derived Obsidian graph adapter payload

Validate quickly:

```bash
python3 scripts/validate_visualizer_contract.py
```
